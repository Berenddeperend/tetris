import {GameSettings} from "./tetris";

export const defaultGameSettings: GameSettings = {
  width: 10,
  height: 20,
  blockSize: 24,
  gridGutterSize: 1,
  gridOverBlocks: true,
  queueScaleFactor: 0.75,
} 